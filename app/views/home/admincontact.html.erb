<section class="contact-logo">
  <p class="contact-title" style="color: #ff7f00">멋쟁이</p>
<div class="contact-typewriter">
    <h1 class="contact-title"><div id="mytext" ></div></h1>
</div>
  <p class="contact-title" style="color: #FFBF00">처럼</p>

  <h4 class="firstpage">
    멋쟁이 사자처럼 <span class="sixth">6기 합격</span> 을 축하합니다!!
  </h4>
  <h4 style="text-align: center;">
      <a class="typewrite" data-period="2000" data-type='[ "재건", "명승", "유림", "혜린(김)", "혜린(남)", "승완", "시현", "종혁", "민승"
      , "수진", "상무", "희성", "동규", "재준", "호설", "현희", "호영", "민정"]'>
        <span class="wrap"></span>
      </a>
    </h4>


</section>
<div class="app-template">

  <table class="table table-hover">
    <thead class="bg-gray-dark">
      <tr>
        <th colspan="1" >Num</th>
        <th colspan="1" >작성자 email</th>
        <th colspan="1">내용</th>
      </tr>
    </thead>
    <tbody>
      <% if @contact.empty? %>
        <p>글이 없습니다.</p>
      <% else %>
        <tr>
      <% @contact.each do |contact| %>
            <th scope="row"><%= contact.id %></th>
            <td><%= contact.email %></td>
            <td><a href="/contact/admincshow/<%= contact.id%>"><%= contact.content %> </a></td>
        </tr>
      <% end %>
      <% end %>
    </tbody>
  </table>
</div>
