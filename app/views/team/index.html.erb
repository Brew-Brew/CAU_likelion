
<section class="team-logo">
  <div class="team-typewriter">
    <h1>WELCOME TO CAU-LION<br>
      멋쟁이 사자처럼 팀 스터디방입니다.</h1>
  </div>
</section>

<div class="app-template">
  <blockquote class="team-blq">
             <h3>2018 중앙대학교 멋쟁이 사자처럼 팀원소개  - <strong>TEAM<%=@teamid%></strong></h3>

  </blockquote>
  <table class="team-table">
    <thead class="team-bg-light-yellow">
      <tr>
        <th colspan="6"><a><i class="xi-emoticon-devil xi-1x"></i>Team <%=@teamid%> 구성원들!<i class="xi-emoticon-devil xi-1x"></i></a></th>
      </tr>
    </thead>

    <thead>
      <th colspan="3" class="team-table-header">멘토</th>
    </thead>

    <tbody>
      <% @admins.each do |user| %>
        <tr>
          <td><%=user.name%>: <%=user.roles.last.name%></td>
        </tr>
      <% end %>
      <th colspan="3" class="team-table-header">팀원</th>
      <% @students.each do |user| %>
        <tr>
          <td><%=user.name%>: <%=user.roles.last.name%></td>
        </tr>
      <% end %>

    </tbody>

</table>
<div class="team-studyButton">
  <%= link_to '스터디룸으로 이동하기', posts_path(@teamid), class: "btn btn-outline-warning" %>
</div>

</div>
